---
import { Image } from "astro:assets";
import secondCTAIcon from "../../assets/secondCTAIcon.svg";
import ZohoFormEmbed from "../landingPage_3/ZohoFormEmbed.astro";
---

<style>
	.ai-cta-section {
		padding: 4rem 1rem;
		margin-bottom: -100px;
		position: relative;
		z-index: 10;
	}

	.ai-cta-card {
		max-width: 1100px;
		max-height: 280px;
		margin: 0 auto;
		background: #2183ed; /* solid base */
		border-radius: 20px;
		padding: 2rem 4rem 2rem 3rem;
		display: grid;
		grid-template-columns: 1.2fr 0.6fr 1fr;
		align-items: center;
		position: relative;
		z-index: 10;
		box-shadow: 0px 10px 40px rgba(0, 0, 0, 0.25);
		overflow: hidden; /* clip pseudo-element */
	}
	.ai-cta-card::before {
		content: "";
		position: absolute;
		inset: 0;
		z-index: 0;

		background-image: url("../../assets/secondCTAbg.png");
		background-size: 49%;
		background-position: right center;
		background-repeat: no-repeat;
		opacity: 0.5;

		pointer-events: none;
	}
	.ai-cta-card > * {
		position: relative;
		z-index: 1;
	}

	.ai-cta-content {
		min-width: 530px;
	}
	/* Left text */
	.ai-cta-content h3 {
		font-weight: 500;
		font-size: 30px;
		line-height: 120%;
		letter-spacing: 0%;
		color: #fff;
	}

	.ai-cta-content p {
		font-weight: 300;
		font-size: 18px;
		line-height: 140%;
		letter-spacing: 0%;
		max-width: 300px;
		color: #fff;
	}

	.ai-cta-btn {
		background: #2183ed;
		color: #fff;
		border: 1px solid #fff;
		padding: 12px 26px;
		border-radius: 30px;
		font-size: 18px;
		font-weight: 500;
		line-height: 28px;
		letter-spacing: 0%;
		cursor: pointer;
		transition:
			transform 0.2s ease,
			box-shadow 0.2s ease;
		max-width: 262px;
	}

	.ai-cta-btn:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 25px rgba(33, 131, 237, 0.35);
	}

	/* Center icon */
	.ai-cta-icon {
		display: flex;
		justify-content: center;
	}

	.ai-cta-icon img {
		width: 70px;
		height: auto;
	}

	/* Right form */
	.ai-cta-form {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		max-width: 327px;
	}

	.ai-cta-form input {
		width: 100%;
		padding: 14px 16px;
		border-radius: 10px;
		border: 2px solid #eeeeee;
		font-size: 15px;
		outline: none;

		font-weight: 400;
		font-size: 16px;
		line-height: 140%;
		letter-spacing: 0%;
		min-width: 327px;
	}

	.ai-cta-form input:focus {
		border-color: #2183ed;
	}

	/* ---------- Responsive ---------- */

	@media (max-width: 992px) {
		.ai-cta-card {
			display: flex;
			flex-direction: column;
			text-align: center;
			padding: 1.5rem;
			gap: 1.5rem;
			height: auto;
			max-height: none;
		}

		.ai-cta-content {
			min-width: 0;
			width: 100%;
		}

		.ai-cta-icon {
			display: none;
		}

		.ai-cta-form {
			width: 100%;
			max-width: 100%;
			align-items: center;
		}

		.ai-cta-form input {
			min-width: 0;
		}

		.ai-cta-btn {
			max-width: 100%;
			width: 100%;
		}

		.ai-cta-card::before {
			background-position: center bottom;
			background-size: 35%;
		}
	}

	@media (max-width: 480px) {
		.ai-cta-content h3 {
			font-size: 22px;
		}

		.ai-cta-btn {
			width: 100%;
		}
	}
</style>
<section class="ai-cta-section">
	<div class="ai-cta-card">
		<!-- Left content -->
		<div class="ai-cta-content">
			<h3>Ready to try the <br />“Ask → Watch → Practice” flow?</h3>
			<p> Let your child ask a question and see how AI breaks it down in minutes.</p>
		</div>

		<!-- Right form -->
		<div class="ai-cta-form">
			<ZohoFormEmbed formIdSuffix="cta2" layout="vertical" />
		</div>

		<!-- Center icon -->
		<div class="ai-cta-icon">
			<Image src={secondCTAIcon} alt="AI Chat Icon" />
		</div>
	</div>
</section>
